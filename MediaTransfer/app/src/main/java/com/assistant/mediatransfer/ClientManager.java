package com.assistant.mediatransfer;

/**
 * manager clients.
 *
 * Basically one client corresponding to one connection. But when reconnecting, connection maybe not
 * available. Beside this, one Client will be available only received one client info event. or
 * only client is authenticated, one connection can be treated as one client.
 */

public class ClientManager {

    // TODO
}
